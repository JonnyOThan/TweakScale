// Changelog file generated by yaclog-ksp (https://git.offworldcolonies.nexus/drewcassidy/yaclog-ksp)
KERBALCHANGELOG
{
    modName = TweakScale
    VERSION {
        version = 3.3.2.1
        versionDate = 2026-05-14
        change = Add support for propellers from KerbalPowersNaval
        change = Fixed exception causing scaling to completely break when InfernalRobotics is installed
        change = Moving a part that has inventory slots in EVA construction now shows the correct number of slots after re-attaching
    }

    VERSION {
        version = 3.3.1
        versionDate = 2025-10-14
        CHANGE {
            type = Prevented Scaling Of Several Problematic Parts
            change = Some additional procedural parts from RO
        }

        CHANGE {
            type = Prevented Scaling Of Several Problematic Parts
            change = Stock fairing since they don't work correctly
        }

        CHANGE {
            type = Prevented Scaling Of Several Problematic Parts
            change = RealChutes
        }

        CHANGE {
            type = Other Changes
            change = Fixed mass scale for RCS parts to maintain TWR
        }

        CHANGE {
            type = Other Changes
            change = Make sure drills are never treated as science so that they can be scaled up
        }

        CHANGE {
            type = Other Changes
            change = Fix NRE when a particle emitter transform is missing
        }

        CHANGE {
            type = Other Changes
            change = Fix exception when TestFlight is installed
        }

        CHANGE {
            type = Other Changes
            change = Gracefully handle harmony patching failure (usually caused by out of date mods)
        }

        CHANGE {
            type = Other Changes
            change = Support changing waterfall plumes with B9PartSwitch
        }

        CHANGE {
            type = Other Changes
            change = Wheel suspension spring strength exponent set to 2 in order to get consistent relative displacement at different scales
        }

        CHANGE {
            type = Other Changes
            change = Added bulkhead size heuristics up to size6 (7.5m)
        }

        CHANGE {
            type = Other Changes
            change = Better support for SystemHeat (thanks @arbsoup)
        }

        CHANGE {
            type = Other Changes
            change = Fix scaling for variable power engines from Near Future Propulsion
        }

        CHANGE {
            type = Other Changes
            change = Optimized application of scaling to PartModules; removed duplicate application of certain scale factors in derived types
        }

        CHANGE {
            type = Other Changes
            change = Specialized scale handlers can now opt to run before the exponent-based scaling
        }

        CHANGE {
            type = Other Changes
            change = Add support for scaling Near Future Exploration antenna reflectors
        }

        CHANGE {
            type = Other Changes
            change = Better support for scaling modules that are altered with B9PartSwitch
        }

        CHANGE {
            type = Other Changes
            change = Fixed scaling of FAR wings
        }

        CHANGE {
            type = Other Changes
            change = Fix incorrectly scaling control surface fraction for stock control surfaces
        }

    }

    VERSION {
        version = 3.2.4
        versionDate = 2025-02-05
        change = Fix attachnode positions on parts where the B9PS subtype has a different position from the prefab
        change = Fix solar panels not scaling power output
        change = Gracefully handle other mods throwing exceptions from PluginConfiguration.CreateForType
        change = Fix default scale on Planetside PD 0625 docking port
        change = Fix ISRU and other ModuleResourceConverter not scaling inputs and outputs correctly
        change = SSPX cupolas (greenhouse, telescope) are no longer treated as science parts so that they can be scaled up
        change = Fix ModuleGeneratorExtended scaling behavior (used by KNES and some other mods)
        change = Fix FNEmitterController module scaling from KSPIE
        change = Gracefully handle certain bad scaling configs
        change = Fix engine particle scaling for certain cases (e.g. Restock Twin Boar)
    }

    VERSION {
        version = 3.2.3
        change = Fixed a bug where the part action window would become unusable after right-clicking certain parts
        change = Fixed "match node size" for Making History KV pod top nodes
        change = Fixed "match node size" for Making History Kodiak engine
    }

    VERSION {
        version = 3.2.2
        change = Moved .version file for ScaleRedist library
        change = Fixed NullReferenceException when trying to scale a part after re-rooting a surface attachment
        change = Fixed NearFutureLaunchVehicles node sizes
    }

}
